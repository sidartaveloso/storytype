export * from './{{pascalCase name}}.types';
{{#if hasMock}}export * from './{{pascalCase name}}.mock';
{{/if}}{{#if hasStories}}export * as Stories from './{{pascalCase name}}.stories';
{{/if}}export { default } from '{{entry}}';
