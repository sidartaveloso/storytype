import type { Meta, StoryObj } from '@storybook/vue3';
import {{pascalCase name}} from './{{pascalCase name}}.vue';
import { generateMockData } from './{{pascalCase name}}.mock';

const meta: Meta<typeof {{pascalCase name}}> = {
  title: '{{#if (eq type "atomos")}}01 - Átomos{{else if (eq type "moleculas")}}02 - Moléculas{{else if (eq type "organismos")}}03 - Organismos{{else if (eq type "templates")}}04 - Templates{{else}}05 - Pages{{/if}}/{{pascalCase name}}',
  component: {{pascalCase name}},
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component: 'Descrição do componente {{pascalCase name}}',
      },
    },
  },
} satisfies Meta<typeof {{pascalCase name}}>;

export default meta;
type Story = StoryObj<typeof meta>;

const mockData = generateMockData();

export const Default: Story = {
  args: {
    ...mockData.props,
    ...mockData.models,
  },
};
