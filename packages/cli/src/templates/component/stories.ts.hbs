import type { Meta, StoryObj } from '@storybook/vue3';
import {{pascalCase name}} from '{{entry}}';
{{#if hasMock}}import { generateMockData } from './{{pascalCase name}}.mock';
{{/if}}

const meta: Meta<typeof {{pascalCase name}}> = {
  title: '{{section}}/{{pascalCase name}}',
  component: {{pascalCase name}},
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component: 'Descrição do componente {{pascalCase name}}',
      },
    },
  },
} satisfies Meta<typeof {{pascalCase name}}>;

export default meta;
type Story = StoryObj<typeof meta>;

{{#if hasMock}}const mockData = generateMockData();

export const Default: Story = {
  args: {
    ...mockData.props,
    ...mockData.models,
  },
};
{{else}}export const Default: Story = {
  args: {
    //TODO: Add args here
  },
};
{{/if}}
