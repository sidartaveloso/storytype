import { describe, it, expect } from 'vitest';
import Header from './Header.vue';

describe('Header', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(Header).toBeDefined();
  });
});
