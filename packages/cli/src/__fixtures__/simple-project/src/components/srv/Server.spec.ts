import { describe, it, expect } from 'vitest';
import Server from './Server.vue';

describe('Server', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(Server).toBeDefined();
  });
});
