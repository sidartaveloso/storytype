import { describe, it, expect } from 'vitest';
import Service from './Service.vue';

describe('Service', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(Service).toBeDefined();
  });
});
